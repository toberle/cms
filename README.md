# CMS

## Docker
`docker-compose up -d`

## Running and debugging Spring Boot
Run Spring Boot as Java application - main class, main method

## Running and debugging Spring Boot using Maven plugin
`spring-boot:run -Dspring-boot.run.fork=false`